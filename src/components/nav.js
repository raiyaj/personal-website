import React from 'react';
import styled from 'styled-components';

const Nav = () => {
  return (
    <NavWrapper>
      raiya.space
    </NavWrapper>
  )
};

const NavWrapper = styled.nav`
  padding: 2rem;
`;

export default Nav;
